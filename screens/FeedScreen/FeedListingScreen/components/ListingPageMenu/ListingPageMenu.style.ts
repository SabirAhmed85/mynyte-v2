import { StyleSheet } from 'react-native';

const styles = () =>
  StyleSheet.create({
    containerAlt: {
      backgroundColor: '#4c4b4b',
      paddingTop: 10,
      paddingBottom: 10
    },
  });

export default styles;
