import { StyleSheet } from 'react-native';

const styles = () =>
  StyleSheet.create({
    containerAlt: {
      backgroundColor: '#373737',
      paddingTop: 10,
      paddingBottom: 10
    },
  });

export default styles;
